stages:
  - build

build_project:
  stage: build
  script:
    - xcodebuild clean -project CarbonFootprintTracking.xcodeproj -scheme SchemeName | xcpretty
    - xcodebuild test -project CarbonFootprintTracking.xcodeproj -scheme SchemeName -destination 'platform=iOS Simulator,name=iPhone 11,OS=13.0' | xcpretty -s
  tags:
    - carbon
